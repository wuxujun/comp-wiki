# Remediation Status — 2026-08-20

> 本文件记录 `.audit/REPORT.md` 基线审计后的处理状态。基线报告保留原始发现，不作追溯性改写。

## 已解决或已缓解

- Canonical 内容根已明确为仓库根 `raw/`、`wiki/`、`mocs/`；`.wiki/` 仅保留为兼容入口。
- 中英文来源政策已改为“`raw/` 保留原文，`wiki/` 默认中文综合”，原先 11 页的语言政策冲突已消除。
- 7 个 PBL 外部来源说明均已补充来源语言、访问日期、稳定标识、本地留存状态和许可边界。
- SIN 的“线上/线下”已拆分为在线交付与本地学校监督，不再标记为矛盾。
- IEO 的“线上/线下”已拆分为赛道、阶段、地区和年份，不再标记为矛盾。
- 6 个综合页的 `raw/docs/` 目录级引用已替换为分类目录和代表性一对一来源说明。
- 389 份原始来源说明均已显式记录“许可/授权状态未知”和保守的再分发边界。
- 通过 HTTP 状态和 ETag 回溯核验补齐 9 个 Thinktown 文件 URL；缺失外部来源的记录由 71 份降至 62 份，并建立逐条核验队列。
- `.gitignore` 已排除 macOS 元数据、Obsidian workspace 和本地 session 状态。

## 仍待处理

- 仓库私密性和 `raw/` 是否允许纳入 Git 尚未确认；未执行暂存或提交。
- 389 份原始来源尚无可验证的实际许可或再分发授权；如需对外发布，仍须由权利主体或材料提供方确认。
- 62 份原始来源仍缺外部发布位置，详见 `.audit/MISSING-EXTERNAL-ORIGINS.md`。
- 7 个 PBL 外部来源和 2 个赛事官方来源仍为 URL/DOI 级留存，没有不可变网页快照或本地全文。
- PBL 每周课外投入仍有一项来源内部矛盾，等待课程提供方澄清。
- BPhO 中高级物理挑战赛的标准版与 Online 版仍需主办方一手材料确认。
- 通用 llm-wiki CLI 的目录和 frontmatter schema 与本仓库不同；在适配或迁移前继续禁止写入和 `--fix` 操作。

## 验证结果

- 扫描 `wiki/` 与 `mocs/` 共 423 个 Markdown 页面。
- 检查 3,439 个相对链接，未发现失效链接。
- 未发现 frontmatter、单一 H1、标题层级或代码围栏错误。
- 当前有 3 个页面保留 `## Contradictions`，对应 2 个不同的未解决问题。
- `git diff --check` 通过；Git 暂存区为空。
